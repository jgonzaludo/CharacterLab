# Copy to web/.env for local development (Vite only exposes vars prefixed with VITE_)
VITE_GEMINI_API_KEY=
# Optional — defaults to gemini-2.5-flash (gemini-1.5-flash often 404s on current API). On 404, the app retries other Flash models.
# VITE_GEMINI_MODEL=gemini-2.0-flash
